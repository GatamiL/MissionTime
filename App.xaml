<Application x:Class="MissionTime.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MissionTime"
             xmlns:infra="clr-namespace:MissionTime.Services"
             StartupUri="Views/DbSelect.xaml">
    <Application.Resources>
        <!-- БАЗОВЫЕ РЕСУРСЫ (ДЛЯ КОДА) -->
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#005A9E"/>
        <FontFamily x:Key="MainFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="MainFontSize" xmlns:sys="clr-namespace:System;assembly=mscorlib">14</sys:Double>

        <!-- РЕСУРСЫ ДЛЯ СТИЛЬНОГО ОКНА (TitleBar Buttons) -->
        <Style x:Key="WindowChromeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4DFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowChromeCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E81123"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F1707A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- НОВЫЙ СУПЕР-СТИЛЬ ДЛЯ ОКОН С КАСТОМНОЙ ШАПКОЙ -->
        <Style x:Key="StandardWindow" TargetType="Window">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="Icon" Value="/Assets/app_icon.png"/>
            
            <!-- Активация WindowChrome -->
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome CaptionHeight="32" 
                                  ResizeBorderThickness="6" 
                                  GlassFrameThickness="0" 
                                  CornerRadius="0"/>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <!-- Главный бордер окна с цветом Темы! -->
                        <Border x:Name="WindowBorder" 
                                BorderBrush="{DynamicResource AccentBrush}" 
                                BorderThickness="1" 
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <!-- Высота заголовка -->
                                    <RowDefinition Height="*" />
                                    <!-- Тело окна -->
                                </Grid.RowDefinitions>

                                <!-- ШАПКА ОКНА (TITLEBAR) -->
                                <Grid x:Name="CustomTitleBar" 
                                      Grid.Row="0" 
                                      Height="32" 
                                      Background="{DynamicResource AccentBrush}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <!-- Иконка -->
                                        <ColumnDefinition Width="*" />
                                        <!-- Заголовок -->
                                        <ColumnDefinition Width="Auto" />
                                        <!-- Кнопки -->
                                    </Grid.ColumnDefinitions>

                                    <!-- Иконка приложения (если есть) -->
                                    <Image x:Name="AppIcon" Grid.Column="0" 
                                           Margin="10,0,5,0" 
                                           Width="16" Height="16" 
                                           VerticalAlignment="Center"
                                           Source="{TemplateBinding Icon}"
                                           SnapsToDevicePixels="True"/>

                                    <!-- Текст заголовка -->
                                    <TextBlock Grid.Column="1" 
                                               Text="{TemplateBinding Title}" 
                                               Foreground="White" 
                                               FontWeight="SemiBold" 
                                               FontSize="12" 
                                               VerticalAlignment="Center" 
                                               Margin="10,0,0,0"
                                               TextTrimming="CharacterEllipsis"/>

                                    <!-- Системные Кнопки -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <!-- СВЕРНУТЬ -->
                                        <Button Style="{StaticResource WindowChromeButtonStyle}" Click="MinimizeButton_Click"
                                                ToolTip="Свернуть">
                                            <Path Data="M0,8 L10,8" Stroke="White" StrokeThickness="1.5" Width="10" Height="8" SnapsToDevicePixels="True"/>
                                        </Button>
                                        
                                        <!-- РАЗВЕРНУТЬ / ВОССТАНОВИТЬ -->
                                        <Button x:Name="MaxBtn" Style="{StaticResource WindowChromeButtonStyle}" Click="MaximizeButton_Click"
                                                ToolTip="Развернуть">
                                            <Path x:Name="MaxPath" Data="M1,1 H9 V9 H1 Z" Stroke="White" StrokeThickness="1.5" Width="10" Height="10" Fill="Transparent" SnapsToDevicePixels="True"/>
                                        </Button>
                                        
                                        <!-- ЗАКРЫТЬ -->
                                        <Button Style="{StaticResource WindowChromeCloseButtonStyle}" Click="CloseButton_Click"
                                                ToolTip="Закрыть">
                                            <Path Data="M1,1 L9,9 M9,1 L1,9" Stroke="White" StrokeThickness="1.5" Width="10" Height="10" SnapsToDevicePixels="True"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- КОНТЕНТ ОКНА -->
                                <AdornerDecorator Grid.Row="1">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                </AdornerDecorator>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- Если окно развернуто: убираем рамку (бордер) чтобы не обрезалось системой -->
                            <Trigger Property="WindowState" Value="Maximized">
                                <Setter TargetName="WindowBorder" Property="BorderThickness" Value="8"/>
                                <Setter TargetName="WindowBorder" Property="BorderBrush" Value="Transparent"/>
                                <!-- Меняем иконку Максимизации на значок двух квадратиков -->
                                <Setter TargetName="MaxPath" Property="Data" Value="M3,1 H9 V7 H3 Z M1,3 H7 V9 H1 Z"/>
                                <Setter TargetName="MaxBtn" Property="ToolTip" Value="Восстановить"/>
                            </Trigger>
                            
                            <!-- Отключаем кнопку Развернуть, если окно имеет фиксированный размер -->
                            <Trigger Property="ResizeMode" Value="NoResize">
                                <Setter TargetName="MaxBtn" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="ResizeMode" Value="CanMinimize">
                                <Setter TargetName="MaxBtn" Property="Visibility" Value="Collapsed"/>
                            </Trigger>

                            <!-- Скрываем иконку, если её нет -->
                            <DataTrigger Binding="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                <Setter TargetName="AppIcon" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
        </Style>

        <!-- СТИЛЬ ДЛЯ СПЕЦИАЛИЗИРОВАННОЙ ГЛАВНОЙ СЕТКИ (dgMainReport) -->
        <!-- ШАБЛОН ДЛЯ ВСЕХ ТАБЛИЦ (С ФИКСОМ БАГА WINDOWS НА ОТРИЦАТЕЛЬНУЮ ШИРИНУ) -->
        <ControlTemplate x:Key="ModernDataGridTemplate" TargetType="DataGrid">
            <Border BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    Background="{TemplateBinding Background}" 
                    CornerRadius="4" 
                    SnapsToDevicePixels="True">
                <ScrollViewer x:Name="DG_ScrollViewer" Focusable="false" CanContentScroll="True">
                    <ScrollViewer.Template>
                        <ControlTemplate TargetType="ScrollViewer">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <!-- БАГФИКС: Прячем эту кнопку совсем, так как заголовки строк скрыты, и убираем багованную привязку ширины -->
                                <Button Command="{x:Static DataGrid.SelectAllCommand}" Focusable="false" Visibility="Collapsed"
                                        Style="{DynamicResource {ComponentResourceKey ResourceId=DataGridSelectAllButtonStyle, TypeInTargetAssembly={x:Type DataGrid}}}"/>
                                
                                <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter" Grid.Column="1" Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" Grid.ColumnSpan="2" Grid.Row="1"/>
                                <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="2" 
                                           Maximum="{TemplateBinding ScrollableHeight}" 
                                           ViewportSize="{TemplateBinding ViewportHeight}"
                                           Orientation="Vertical" Grid.Row="1" 
                                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" 
                                           Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="1" Grid.Row="2"
                                            Maximum="{TemplateBinding ScrollableWidth}" 
                                            ViewportSize="{TemplateBinding ViewportWidth}"
                                            Orientation="Horizontal" 
                                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" 
                                            Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Grid>
                        </ControlTemplate>
                    </ScrollViewer.Template>
                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                </ScrollViewer>
            </Border>
        </ControlTemplate>

        <!-- СТИЛЬ ДЛЯ СПЕЦИАЛИЗИРОВАННОЙ ГЛАВНОЙ СЕТКИ (dgMainReport) -->
        <Style x:Key="MainReportGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E6EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F3F5"/>
            <Setter Property="MinRowHeight" Value="32"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="SelectionMode" Value="Extended"/>
            <Setter Property="SelectionUnit" Value="Cell"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False"/>
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
            <Setter Property="VirtualizingPanel.CacheLength" Value="2"/>
            <Setter Property="VirtualizingPanel.CacheLengthUnit" Value="Page"/>
            <!-- Применяем фикс шаблона сюда тоже -->
            <Setter Property="Template" Value="{StaticResource ModernDataGridTemplate}"/>
        </Style>

        <!-- ОБЩИЙ СТИЛЬ ДЛЯ ОСТАЛЬНЫХ ТАБЛИЦ ПРИЛОЖЕНИЯ -->
        <Style x:Key="StandardDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <!-- Стильная чересполосица (зебра) -->
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E9ECEF"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="MinRowHeight" Value="36"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False"/>
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
            <Setter Property="VirtualizingPanel.CacheLength" Value="1"/>
            <Setter Property="VirtualizingPanel.CacheLengthUnit" Value="Page"/>
            <Setter Property="Template" Value="{StaticResource ModernDataGridTemplate}"/>
        </Style>

        <!-- Стиль для заголовков колонок (шапка) -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F3F5"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
        </Style>

        <!-- Стиль для ячеек -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" 
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E7F1FF"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F1F3F5"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter Property="Background" Value="#E9ECEF"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#ADB5BD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Separator" x:Key="{x:Static MenuItem.SeparatorStyleKey}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="#E2E6EA" Margin="25,4,5,4"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- БАЗОВЫЙ СТИЛЬ ДЛЯ ВСЕХ ТЕКСТОВЫХ БЛОКОВ -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>

        <Style x:Key="HeaderResultText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="StandardText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="LineHeight" Value="20"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="HintItalicText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="StandardTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StandardComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            
            <!-- РЕСУРСЫ ВНУТРИ КОМБОБОКСА -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="Transparent" BorderThickness="0" Padding="{TemplateBinding Padding}" CornerRadius="2" Margin="2,1">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#F1F3F5"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Кастомный триггер разворачивания (визуальная обертка) -->
                            <ToggleButton x:Name="ToggleButton" 
                                          Grid.Column="2" 
                                          Focusable="false" 
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}" 
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}" 
                                                BorderThickness="1" 
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="30" />
                                                </Grid.ColumnDefinitions>
                                                <!-- Стрелочка -->
                                                <Path x:Name="Arrow" Grid.Column="1" Fill="#6C757D" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                <!-- Переворачиваем стрелку -->
                                                <Setter TargetName="Arrow" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="180" CenterX="4" CenterY="2"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <!-- Контент (выбранное значение) -->
                            <ContentPresenter Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="10,0,30,0" 
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left" />

                            <!-- ПОЛЕ ДЛЯ ВВОДА ТЕКСТА (для автозаполнения и IsEditable="True") -->
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Focusable="True"
                                     Margin="10,0,30,0"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     HorizontalAlignment="Stretch"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     FontSize="{TemplateBinding FontSize}"
                                     FontFamily="{TemplateBinding FontFamily}"
                                     Visibility="Collapsed">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox">
                                        <!-- Обертка, через которую WPF рендерит текст в PART_EditableTextBox -->
                                        <Border x:Name="PART_ContentHost" Focusable="False" Background="Transparent" />
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            
                            <!-- Выпадающий список -->
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#DEE2E6" CornerRadius="4" Margin="0,2,0,4">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Direction="270" Opacity="0.1"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                                <Setter Property="IsTabStop" Value="False"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinHeight" Value="35"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#AAB0B6"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E2E6EA"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#D2D6DA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#F1F3F5"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#DEE2E6"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="BorderBrush" Value="#DC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#A71D2A"/>
                    <Setter Property="BorderBrush" Value="#A71D2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ИМПЛИЦИТНЫЙ СТИЛЬ ПО УМОЛЧАНИЮ ДЛЯ ВСЕХ КНОПОК В ПРИЛОЖЕНИИ -->
        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}"/>

        <!-- ИМПЛИЦИТНЫЕ СТИЛИ ДЛЯ ПОЛЕЙ ВВОДА -->
        <Style TargetType="TextBox" BasedOn="{StaticResource StandardTextBox}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource StandardComboBox}"/>

        <!-- СТИЛЬ ДЛЯ ВНУТРЕННЕГО ТЕКСТБОКСА КАЛЕНДАРЯ -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <Border Background="Transparent"/>
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center" 
                                        Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ДЛЯ DATEPICKER (КАЛЕНДАРЬ ВВОДА) -->
        <Style TargetType="DatePicker">
            <Setter Property="FontSize" Value="{DynamicResource MainFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource MainFontFamily}"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="MainBorder" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                SnapsToDevicePixels="True">
                            <Grid x:Name="PART_Root" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Поле Текста -->
                                <DatePickerTextBox x:Name="PART_TextBox" 
                                                   Grid.Column="0" 
                                                   Focusable="{TemplateBinding Focusable}" 
                                                   HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                   VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                
                                <!-- Кнопка вызова календаря -->
                                <Button x:Name="PART_Button" 
                                        Grid.Column="1" 
                                        Focusable="False" 
                                        Padding="8,0,10,0"
                                        Cursor="Hand"
                                        Background="Transparent"
                                        BorderThickness="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                                <!-- Векторная иконка календаря (Modern SVG Path) -->
                                                <Path x:Name="Glyph" Data="M19,4H18V2H16V4H8V2H6V4H5C3.89,4 3,4.9 3,6V20C3,21.1 3.89,22 5,22H19C20.1,22 21,21.1 21,20V6C21,4.9 20.1,4 19,4M19,20H5V10H19V20M19,8H5V6H19V8M12,13H17V18H12V13Z" 
                                                      Fill="#6C757D" Stretch="Uniform" Width="16" Height="16" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                
                                <!-- Всплывающее окно календаря -->
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#ADB5BD"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 1. ОБЩИЙ СТИЛЬ ДЛЯ ВСЕХ КНОПОК (БАЗОВЫЙ ДЛЯ ТУЛБАРОВ) -->
        <Style x:Key="BaseButtonStyle" TargetType="ButtonBase">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ButtonBase">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F1F3F5"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E9ECEF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.6"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 2. ОБЫЧНАЯ КНОПКА -->
        <Style x:Key="StandardButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}"/>

        <!-- 3. ПЕРЕКЛЮЧАТЕЛЬ (Теперь ошибки не будет!) -->
        <Style x:Key="StandardToggleButton" TargetType="ToggleButton" BasedOn="{StaticResource BaseButtonStyle}">
            <Style.Triggers>
                <!-- СОСТОЯНИЕ: ВСЁ РАЗВЕРНУТО (IsChecked = True) -->
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Content" Value="Свернуть все"/>
                </Trigger>

                <!-- СОСТОЯНИЕ: ВСЁ СКРЫТО (IsChecked = False) -->
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Content" Value="Развернуть все"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Цвет для выходных -->
        <SolidColorBrush x:Key="WeekendForeground" Color="#D32F2F"/>
        <SolidColorBrush x:Key="HeaderBackground" Color="#FDFDFD"/>

        <!-- СТИЛЬ ЗАГОЛОВКА ДНЯ -->
        <!-- СТИЛЬ ДЛЯ СОВРЕМЕННОГО TREEVIEW -->
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" Height="16" Width="16">
                            <!-- Шенилька (Стрелочка) -->
                            <Path x:Name="ExpandPath" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" 
                                  Data="M 0 0 L 4 4 L 0 8" 
                                  Stroke="#6C757D" 
                                  StrokeThickness="1.5"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <!-- Поворачиваем стрелочку на 45 градусов (вниз-вправо) или 90 -->
                                <Setter TargetName="ExpandPath" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="45"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ExpandPath" Property="Data" Value="M 0 0 L 4 4 L 8 0"/>
                                <Setter TargetName="ExpandPath" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ExpandPath" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="Padding" Value="5,4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <!-- Переключатель (Стрелочка) -->
                            <ToggleButton x:Name="Expander" 
                                          Style="{StaticResource ExpandCollapseToggleStyle}" 
                                          IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press"/>
                            
                            <!-- Заголовок элемента (Фон под текстом) -->
                            <Border x:Name="Bd" 
                                    Grid.Column="1" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Padding="{TemplateBinding Padding}" 
                                    CornerRadius="3" 
                                    SnapsToDevicePixels="true"
                                    Margin="2,1">
                                <ContentPresenter x:Name="PART_Header" 
                                                  ContentSource="Header" 
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            
                            <!-- Дочерние элементы -->
                            <ItemsPresenter x:Name="ItemsHost" 
                                            Grid.Column="1" 
                                            Grid.Row="1" 
                                            Margin="10,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" SourceName="Bd" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#F1F3F5"/>
                            </Trigger>
                            <!-- Чтобы ховер не затирал селекшн -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" SourceName="Bd" Value="true"/>
                                    <Condition Property="IsSelected" Value="true"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TreeView">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style x:Key="DayHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="Background" Value="{StaticResource HeaderBackground}"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel>
                            <!-- Число -->
                            <TextBlock Text="{Binding DayNumber}" 
                                       FontSize="12"
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center"/>
                            <!-- День недели -->
                            <TextBlock Text="{Binding DayName}" 
                                       FontSize="10" 
                                       Margin="0,2,0,0"
                                       Opacity="0.8" 
                                       HorizontalAlignment="Center"
                                       Foreground="{Binding DayColor}"/>
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ОБНОВЛЕННЫЙ СТИЛЬ СТРОКИ -->
        <Style x:Key="DayRowStyle" TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <!-- Если это сотрудник: красим фон в светло-серый и делаем текст жирным -->
                <DataTrigger Binding="{Binding RowType}" Value="Employee">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>

                <!-- Если это работа: можно сделать текст обычным или чуть сместить (но отступ лучше делать в данных) -->
                <DataTrigger Binding="{Binding RowType}" Value="Work">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="FontStyle" Value="Italic"/>
                </DataTrigger>

                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="4" 
                                Margin="2,1"
                                SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#F1F3F5"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <!-- Hover + Selected -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsMouseOver" Value="true"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </MultiTrigger>

                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <!-- Скругление всего списка -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Name="Border" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                SnapsToDevicePixels="true">
                            <ScrollViewer Margin="0" Focusable="false">
                                <ItemsPresenter Margin="{TemplateBinding Padding}"/>
                            </ScrollViewer>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="Border" Property="Background" Value="#F8F9FA"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#E9ECEF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ТЕКСТА ВНУТРИ ЯЧЕЕК -->
        <Style x:Key="CenterTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2,0"/>
            <!-- Добавь это для четкости шрифта -->
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <Style x:Key="DayCellStyle" TargetType="DataGridCell">
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                        BorderThickness="0" 
                        SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <DataTrigger Binding="{Binding RowType}" Value="Employee">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="Foreground" Value="#6C757D"/>
                    <Setter Property="Focusable" Value="True"/> 
                </DataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RowType}" Value="Work"/>
                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFF9C4"/>
                    <Setter Property="Foreground" Value="Black"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WeekendTextStyle" TargetType="TextBlock" BasedOn="{StaticResource CenterTextStyle}">
            <Setter Property="Foreground" Value="{StaticResource WeekendForeground}"/>
        </Style>

        <!-- Конвертер для скрытия/показа текста "Уволен" -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <infra:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>

    </Application.Resources>
</Application>
