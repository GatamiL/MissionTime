<Window x:Class="MissionTime.Views.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Экспорт данных" Height="320" Width="410" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{StaticResource StandardWindow}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Экспорт данных для отправки" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" Text="Выберите период времени, за который необходимо выгрузить отработанные часы." TextWrapping="Wrap" Foreground="#6C757D" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Год:" Margin="0,0,0,5" Foreground="#495057"/>
                    <ComboBox x:Name="cbYear"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Месяц:" Margin="0,0,0,5" Foreground="#495057"/>
                    <ComboBox x:Name="cbMonth">
                        <ComboBoxItem Content="Январь"/>
                        <ComboBoxItem Content="Февраль"/>
                        <ComboBoxItem Content="Март"/>
                        <ComboBoxItem Content="Апрель"/>
                        <ComboBoxItem Content="Май"/>
                        <ComboBoxItem Content="Июнь"/>
                        <ComboBoxItem Content="Июль"/>
                        <ComboBoxItem Content="Август"/>
                        <ComboBoxItem Content="Сентябрь"/>
                        <ComboBoxItem Content="Октябрь"/>
                        <ComboBoxItem Content="Ноябрь"/>
                        <ComboBoxItem Content="Декабрь"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </StackPanel>

        <UniformGrid Grid.Row="3" Columns="2" Rows="1">
            <Button Content="Выгрузить в файл..." Click="btnExport_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,5,0"/>
            <Button Content="Отмена" Click="btnClose_Click" Style="{StaticResource SecondaryButton}" Margin="5,0,0,0"/>
        </UniformGrid>
    </Grid>
</Window>
